apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
<<<<<<< HEAD
    name: doc-writer
  name: doc-writer
=======
    name: {name}
    app: rw
  name: {name}
>>>>>>> master
spec:
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        name: doc-writer
    spec:
      containers:
      - name: doc-writer
        image: vizzuality/doc-writer
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "0.5"
            memory: "256M"
          limits:
            cpu: "1"
            memory: "512M"
        args:
          - start
        env:
          - name: PORT
            value: "7400"
          - name: NODE_ENV
            value: prod
          - name: NODE_PATH
            value: app/src
          - name: ELASTIC_URL
            valueFrom:
                secretKeyRef:
                  name: dbsecrets
                  key: ELASTIC_URI
          - name: RABBITMQ_URL
            valueFrom:
                secretKeyRef:
                  name: dbsecrets
                  key: RABBITMQ_URI
        ports:
            - containerPort: 7400
        readinessProbe:
          httpGet:
            scheme: HTTP
            path: /healthcheck
            port: 7400
          initialDelaySeconds: 30
          timeoutSeconds: 5
          periodSeconds: 15
        livenessProbe:
          httpGet:
            scheme: HTTP
            path: /healthcheck
            port: 7400
          failureThreshold: 3
          initialDelaySeconds: 30
          timeoutSeconds: 5
          periodSeconds: 15

      restartPolicy: Always
